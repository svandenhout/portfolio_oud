function animateOver(element) {
    var ani1 = {type: 'fontSize', from: 18, to: 72, step: 1, delay: 8};
    $fx(element).fxAdd(ani1).fxRun();
}

function animateOver(element) {
    var ani1 = {type: 'fontSize', from: 72, to: 18, step: 1, delay: 8};
    $fx(element).fxAdd(ani1).fxRun();
}